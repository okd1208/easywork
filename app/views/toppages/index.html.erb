   <% if logged_in? %>
        <h2 class="text-center">ようこそ <%= current_user.name %> さん</h2>
        
        <div class="row">
            <aside class="col-sm-4">
                <%= form_with(model: @Ptask, local: true) do|f| %>
                    <div class="form-group">
                        <label>タスク内容</label>
                        <%= f.text_area :task, class: 'form-control', rows: 5 %>
                        <label>カテゴリー</label>
                        <!--<%= f.text_field :category, class: 'form-control' %>-->
                        <input name="category" class="form-control">
                        <label>期限設定</label>
                        <%= f.text_field :deadline, class: 'form-control date-picker', :placeholder => "2020-12-31" %>
                        
                        
                    </div>
                    <%= f.submit '登録する', class: 'btn btn-primary btn-block' %>
                <% end %>
                    <%= link_to '詳細ページ' , "users/#{@user.id}" %>
            </aside>
            
            <div class="col-sm-6">
                <%= render 'layouts/tasks' %>
            </div>
        </div>
    <% else %>
    <div class="text-center">
        <h1 class="mt-5">手軽にタスク管理する</h1>
        <h1 class="mb-5">EasyWork</h1>
        <button type="button" class="btn btn-success w-25"><%= link_to '新規登録', signup_path, :style => "color: white;"  %></button>
        <button type="button" class="btn btn btn-info w-25"><%= link_to  'ログイン', login_path, :style => "color: white;"  %></button>
    </div>
    <% end %>

